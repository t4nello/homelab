services:
  adguard:
    container_name: adguard
    image: adguard/adguardhome:latest
    hostname: adguard
    networks:
      - applications
    labels:
      - "logaggregation=yes"
      - "traefik.enable=true"
      - "traefik.http.routers.adguard-setup.rule=Host(`adguard.${HOST}`) && PathPrefix(`/setup`)"
      - "traefik.http.routers.adguard-setup.service=setup-service"
      - "traefik.http.services.setup-service.loadbalancer.server.port=3000"

      - "traefik.http.routers.adguard-main.rule=Host(`adguard.${HOST}`)"
      - "traefik.http.routers.adguard-main.service=main-service"
      - "traefik.http.services.main-service.loadbalancer.server.port=80"
      - "traefik.docker.network=applications"
    ports:
      - "53:53/tcp"
      - "53:53/udp"
    volumes:
      - adguard_config:/opt/adguardhome/conf
      - adguard_work:/opt/adguardhome/work
    restart: unless-stopped
    #healthcheck:
    #  test: "/bin/netstat -pant | /bin/grep 53"
    #  interval: 45s
    #  timeout: 30s
    #  retries: 3

networks:
  applications:
    external: true

volumes:
  adguard_config:
    name: adguard_config
  adguard_work:
    name: adguard_work
